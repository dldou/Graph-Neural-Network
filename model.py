import torch
import torch.nn as nn
from model_utils import *

class NaiveEncoderPlusGNN(torch.nn):

    def __init__(self, in_channels, output_channels):
        super().__init__()
        raise NotImplementedError

        